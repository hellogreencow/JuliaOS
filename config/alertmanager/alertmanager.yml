route:
  receiver: 'default'
  group_wait: 10s
  group_interval: 5m
  repeat_interval: 3h

receivers:
  - name: 'default'
    webhook_configs:
      - url: 'http://localhost:9999/alert-webhook'
        send_resolved: true